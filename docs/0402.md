增加白名单能力
增加实验配置
增加外部调用
丰富粗排逻辑


校准文件设置，支持多种条件组合


1. 降低目前打分mock服务的base_score为0.1，目前的分数太高了
2. 将max_claim_per_request，score_threshold从配置读取改为请求传参传入，业务调用方可控制优惠券数量和阈值
3. 兜底策略改为先读redis获取兜底分，读不到再取配置中的固定值
4. 新增一个外部打分服务，新增请求字段external，这是一个int字段，传0代表走我们的打分服务，传1代表请求外部打分服务，仿照目前的mock服务开发一个外部mock服务，接口使用http，不支持rpc，base_score先设置为0.2。
5. 请求外部服务时，新增逻辑，会将用户id进行加密，从而保证不被外部服务获取原始的用户id
6. 新增请求字段reqId，这个由业务方填写字符串，无实际意义，只是用于标记请求方便后续debug和排查
7. 新增日志系统，逐步完善，使用字段route标记请求了内部还是外部打分服务，route=1代表内部服务，route=2代表外部服务。这个route字段不会下发给打分服务，用于日志记录，每次请求来了之后，打印一条info日志，包括reqId，用户id，item的id和route、场景id。
8. 场景与route路由是隔离的，就算是external传1，也要正常计算场景划分

approval_policy = "on-request"
sandbox_mode = "workspace-write"
